# Issue Tracker: GitHub

Issues live in the repo's GitHub Issues. Uses the `gh` CLI.

**Remote**: `origin` → https://github.com/harshtohai/skilltrack

## PRs as request surface
**Off** — external PRs are not triaged as incoming requests. Flip to `on` in this file if that changes.